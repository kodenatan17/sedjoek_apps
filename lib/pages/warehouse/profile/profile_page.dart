import 'package:flutter/material.dart';

class ProfilePageWarehouse extends StatelessWidget {
  const ProfilePageWarehouse({Key? key}) : super(key: key);

  Widget build(BuildContext context) {
    return Scaffold();
  }
}

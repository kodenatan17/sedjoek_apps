import 'package:flutter/material.dart';

class NotificationPageWarehouse extends StatelessWidget {
  const NotificationPageWarehouse({Key? key}) : super(key: key);

  Widget build(BuildContext context) {
    return Scaffold();
  }
}

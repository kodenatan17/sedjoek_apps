import 'package:flutter/material.dart';

class ChatPageWarehouse extends StatelessWidget {
  const ChatPageWarehouse({Key? key}) : super(key: key);

  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}

import 'package:flutter/material.dart';

class ChatPageTechnition extends StatelessWidget {
  const ChatPageTechnition({Key? key}) : super(key: key);

  Widget build(BuildContext context){
    return Scaffold();
  }
}

import 'package:flutter/material.dart';

class NotificationPageTechinition extends StatelessWidget {
  const NotificationPageTechinition({Key? key}) : super(key: key);

  Widget build(BuildContext context) {
    return Scaffold();
  }
}

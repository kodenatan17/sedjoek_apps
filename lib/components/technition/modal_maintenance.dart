import 'package:flutter/material.dart';

class ModalMaintenance extends StatelessWidget {
  const ModalMaintenance({Key?key}):super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
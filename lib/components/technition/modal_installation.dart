import 'package:flutter/material.dart';

class ModalInstallation extends StatelessWidget {
  const ModalInstallation({
    Key? key,
  }) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}

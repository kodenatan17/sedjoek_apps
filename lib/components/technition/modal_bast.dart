import 'package:flutter/material.dart';

class ModalBast extends StatelessWidget {
  const ModalBast({Key?key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}
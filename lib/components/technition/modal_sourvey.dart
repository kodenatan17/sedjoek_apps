import 'package:flutter/material.dart';

class ModalSourvey extends StatelessWidget {
  const ModalSourvey({Key?key}) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Scaffold(
      appBar: AppBar(),
    );
  }
}